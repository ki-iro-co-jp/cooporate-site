---
import BaseLayout from "@components/base/BaseLayout.astro";
import AppCard from "@components/apps/Card.astro";
import Pagination from "@components/common/Pagination.astro";
import PageHeader from "@components/common/PageHeader.astro";
import type { AppsEntry } from "@/types";

interface Props {
  entryIndex: AppsEntry;
  entries: AppsEntry[];
  pageIndex: number;
  pageCount: number;
}

const { entryIndex, entries, pageIndex, pageCount }: Props = Astro.props;
---

<BaseLayout
  title={entryIndex.data.title}
  image={entryIndex.data.image?.src}
  description={entryIndex.data.description}
>
  <PageHeader title={entryIndex.data.title} />
  <section class="section m-2">
    <div class="container">
      <div class="row">
        {
          entries.map((entry) => (
            <div class="md:col-4 lg:col-3 sm:col-6 h-min p-2">
              <AppCard entry={entry} />
            </div>
          ))
        }
      </div>
      <Pagination
        collection="apps"
        pageIndex={pageIndex}
        pageCount={pageCount}
      />
    </div>
  </section>
</BaseLayout>
